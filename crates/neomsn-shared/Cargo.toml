[package]
name = "neomsn-shared"
version = "0.1.0"
edition = "2024"

[features]
default = []
widgets = ["dep:iced"]

[dependencies]
uuid = { workspace = true }
serde = { workspace = true }
iced = { version = "0.13", optional = true, features = ["image"] }
